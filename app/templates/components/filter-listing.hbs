{{yield}}


City: {{input class=search-box value=filter key-up=(action 'autoComplete' filter)}}<button class='search'{{action 'search'}}>Search</button>

<ul>
{{#each filteredList as |item|}}
  <li {{action 'choose' item.city}}>{{item.city}}</li>
{{/each}}
</ul>